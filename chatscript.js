function sendMessage() {
    var userInput = document.getElementById("userInput").value;

    // Call ChatGPT API with userInput and update chat box with response
    // Example API call: fetch('https://datugoku.chat/api/chat', { method: 'POST', body: JSON.stringify({ message: userInput }) })
    // Update chat box with response using document.getElementById("chatBox").innerHTML
}